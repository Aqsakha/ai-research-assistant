[build]
  command = "npm run build -- --configuration production && echo '=== Build output ===' && find dist -type f | head -20"
  publish = "dist/ai-research-assistant-frontend"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
